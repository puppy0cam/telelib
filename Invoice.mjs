import { Bot } from "./_internals.mjs";
/** This object contains basic information about an invoice. */
export class Invoice extends Bot {
    constructor(data, token) {
        "use strict";
        super(data, token);
    }
}
