import { Bot } from "./_internals.mjs";
/** This object describes the position on faces where a mask should be placed by default. */
export class MaskPosition extends Bot {
    constructor(data, token) {
        "use strict";
        super(data, token);
    }
}
