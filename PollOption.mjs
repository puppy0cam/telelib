import { Bot } from "./_internals.mjs";
/** This object contains information about one answer option in a poll. */
export class PollOption extends Bot {
    constructor(data, token) {
        "use strict";
        super(data, token);
    }
}
